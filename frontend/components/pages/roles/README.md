# Role pages

Each role owns its page wrappers in this directory. Keep role-specific API calls and UI changes inside the matching role folder to reduce merge conflicts between teams.

Shared UI should live in `../shared` or smaller shared components. Do not copy large page implementations unless the role truly needs a different screen.
